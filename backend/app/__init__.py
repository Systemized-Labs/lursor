"""Lursor backend application package."""

# Bump this with frontend/package.json, backend/pyproject.toml and backend/uv.lock.
# The release gate in .github/workflows/release.yml fails if they disagree.
__version__ = "0.1.9"
